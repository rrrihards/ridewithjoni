import React from "react";
import ReactDOM from "react-dom/client";
import JoniPage from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <JoniPage />
  </React.StrictMode>
);
